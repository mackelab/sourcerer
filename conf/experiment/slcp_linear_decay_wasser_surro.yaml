# @package _global_
defaults:
  - override /simulator: slcp
  - override /source: wasserstein
  - override /surrogate: load_surrogate

source:
  linear_decay_steps: 500 

surrogate:
  surrogate_path: "../results_sourcerer/surrogates/slcp_surrogate.pt"
