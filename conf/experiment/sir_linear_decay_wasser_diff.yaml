# @package _global_
defaults:
  - override /simulator: sir
  - override /source: wasserstein
  - override /surrogate: no_surrogate

source:
  linear_decay_steps: 50
  learning_rate: 0.001
  early_stopping_patience: 100