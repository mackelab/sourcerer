# @package _global_
defaults:
  - override /simulator: inverse_kinematics
  - override /source: mll
  - override /surrogate: load_surrogate

source:
  linear_decay_steps: 500 

surrogate:
  surrogate_path: "../results_sourcerer/surrogates/ik_surrogate.pt"
